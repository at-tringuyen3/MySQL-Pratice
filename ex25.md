### Câu 25. Lấy 5 blog và 5 news mới nhất đã active
```
(SELECT id, title, content
FROM blog WHERE is_active = 1
ORDER BY id DESC
LIMIT 5)
UNION ALL
(SELECT id, title, content
FROM news
WHERE is_active = 1
ORDER BY id DESC
LIMIT 5)
```